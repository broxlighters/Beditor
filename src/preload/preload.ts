// import {ipcRenderer} from  'electron';
// import os from "os";
//
// console.log(os.type());
// ipcRenderer.send('os', os.type());

// import { contextBridge, ipcRenderer } from 'electron';
//
// contextBridge.exposeInMainWorld('message',{
//     loadPreferences: () => ipcRenderer.send('message', 'ping ping ping from render process')
// })
